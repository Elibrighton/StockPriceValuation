<Window x:Class="StockPriceValuation.Views.StockPriceValuationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockPriceValuation"
        mc:Ignorable="d"
        Title="Stock Price Valuation" Height="450" Width="800">
    <Window.DataContext>
        <local:StockPriceValuationViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Margin="10" Command="{Binding GetPriceToBuyButtonCommand}">Get price to buy</Button>
        <Label Grid.Column="0" Grid.Row="1" >Price to buy:</Label>
        <Label Grid.Column="1" Grid.Row="1" Content="{Binding LblPriceToBuyContent, UpdateSourceTrigger=PropertyChanged}"></Label>
    </Grid>
</Window>
